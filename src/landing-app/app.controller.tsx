import { ThemeType } from "./app.params";
import { observable, action } from 'mobx';

export class AppController {
    // @observable themeProvider: ThemeType = {
    //     bodySize: '5px',
    //     primaryColor: 'red',
    //     secondaryColor: 'blue',
    //     subtitleSize: '14px',
    //     titleSize: '18px'
    // };

    // @action setThemeProvider = (theme: ThemeType) => {
    //     this.themeProvider = theme;
    // }

    // async init() {

    // }
}
